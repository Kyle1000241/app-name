import React from "react";
import { Text, StyleSheet, View, Button, TouchableOpacity } from "react-native";

const MenuScreen = () => {
    console.log(props.navigation);
    return (
        <View>
            <Text style = {styles.text}>Welcome to menu screen</Text>
            <Button title="Go to List Screen"
            onPress={() => props.navigation.navigate("List")}/>

            <Button
            style={styles.btn}
            title="Go to Students Screen"
             onPress={() => props.navigation.navigate("Student")}/>




            <TouchableOpacity 
            style={styles.button}
            onPress={() => props.navigation.navigate("Exercise")}/>

            <Text style = {styles.text}>Go to Exercise screen</Text>

            <TouchableOpacity 
            style={styles.button}
            onPress={() => props.navigation.navigate("Name")}/>

            <Text style = {styles.text}>Go to Name screen</Text>



        </View>
    );
};

const styles = StyleSheet.create({
    text: {
        textAlign: "center",
        fontSize: 20,
        marginVertical: 20,
    },
       button:{
        backgroundColorL:"Yellow",
        marginVertical: 15,
        borderRadius: 9,
        marginHorizontal: 20,
        paddingVertical: 20,
        paddingHorizontal: 25,
        width: "85%",
        alignItems: "center"
    },
});

export default MenuScreen;